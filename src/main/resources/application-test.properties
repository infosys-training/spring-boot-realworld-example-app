spring.datasource.url=jdbc:sqlite::memory:
# Only run schema migrations for tests, not seed data
spring.flyway.locations=classpath:db/migration
jwt.secret=test-secret-key-that-is-long-enough-for-hs512-algorithm-testing-only
jwt.sessionTime=3600
image.default=https://static.productionready.io/images/smiley-cyrus.jpg
